package config

const (
	// ServicePort = 0 // Type your prefered service port as an integer
	// ServiceName = "" // Type your prefered service name as a string
	// SecretKeyExample = "" // Type your secret key as a string
)
