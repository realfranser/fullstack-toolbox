const HOST = 'http://localhost';
const PORT = 18000;
const BASE_URL = `${HOST}:${PORT}/api`;

export const API = {
  V1_PRODUCT: `${BASE_URL}/v1/product`,
  V1_PRODUCTS: `${BASE_URL}/v1/products`,
  V1_USERS: `${BASE_URL}/v1/users`,
};
